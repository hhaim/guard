# Status-only scenario (schema v2). Sim/zones/expect come from CLI flags.
schema_version: 2
status:
  - soldier: s2
    state: away
    from: { plan_day: -1, time: "05:00" }
    until: { plan_day: 0, time: "12:00" }
